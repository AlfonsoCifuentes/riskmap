"""Simple dummy translation function used in tests."""

def libre_translate(text: str, source: str = 'auto', target: str = 'en') -> str:
    """Return the original text unchanged (stub)."""
    return text
